# Retro 2026-05-18 — composite_signal + исправление ошибки

## Сделано

- **composite_signal построена** (план 31): стратегия принципа 3 из
  готовых примитивов (funding_extreme + liquidation_sweep →
  aggregate_extended_signals 2-of-3 + OI-gate + RiskEngine), 6 тестов,
  wired в run_backtest/walk_forward/live_runner. Гейты зелёные.

## Моя ошибка и исправление (главное)

Планы 28/31: «composite нельзя валидировать на истории → сразу демо».
**Неверно.** `parsers/coinglass/backfill.py` тянет историю liq/OI/CVD/
funding — composite можно честно бэктестить. Я недоисследовал и дал
неверную вводную, на которой чуть не построили слепой демо-запуск
(=ошибка D3, которую сами документировали).

Исправлено планом 32: правильный порядок — backfill → backtest → WF →
и только при прохождении критерия iter#4 → демо.

## Почему демо НЕ включено

1. **Дисциплина:** слепой forward непроверенной стратегии запрещён;
   валидация на истории теперь известно-возможна → обязана быть первой.
2. **Окружение:** в этом cloud-контейнере нет `COINGLASS_API_KEY` (нет
   `.env`, эфемерный clone). Backfill/демо запускаются в окружении с
   ключом (локально/VPS). «Врубить» отсюда нельзя — не имитирую.

## Честный статус

Код turnkey: composite в live_runner как безопасный no-op (как
liquidation_reversal — без Coinglass-провайдеров не торгует). Демо =
VPS-деплой + Coinglass-live-wiring ПОСЛЕ backfill-валидации.

## Что нужно от пользователя / следующий шаг

В окружении с `COINGLASS_API_KEY` (VPS или локально):
1. `python -m parsers.coinglass.backfill` ETH/BTC (≥12 мес).
2. composite backtest + полный WF (план 32.2).
3. Прошёл iter#4 → forward-test демо (дизайн плана 31). Нет →
   зафиксировать, не натягивать.

Прод/D3 не тронуты. Дисциплина: ошибку признал, путь исправил,
имитацию «запуска» делать не стал.
